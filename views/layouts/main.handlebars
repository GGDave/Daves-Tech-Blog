<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daves Tech Blog</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">home</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                {{!--handlebars dynamic content allows conditions. if logged in it will direct to dashboard
                all other conditions will redirect to logging in  --}}
                <a href="/dashboard">dashboard</a>
                <button id="logout" class="btn-no-style logout">logout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <div class="site-row">
            <div class="site-name">
                <h1>Daves Tech Blog</h1>
            </div>
        </div>
        <main>
            {{{ body }}}
        </main>
    </div>

    {{#if loggedIn}}{{!--if the user is logged in the logout.js script will load. changing the log in button to log out  --}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>

{{!--  --}}